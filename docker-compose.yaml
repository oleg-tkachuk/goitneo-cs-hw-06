services:
  servers:
    build: .
    ports:
      - "3000:3000"
      - "5000:5000/udp"
    networks:
      - common_net
    depends_on:
      - mongo
  mongo:
    image: mongo:latest
    container_name: mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD}
    ports:
      - "27017:27017"
    networks:
      - common_net
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongo ${MONGO_HOST}:${MONGO_PORT}/test --quiet
      interval: 5s
      timeout: 10s
      retries: 10

volumes:
  mongo_data:
    driver: local

networks:
  common_net:
    driver: bridge
